[pytest]
# Pytest configuration for Mamba BDD tests
# Note: VS Code doesn't natively support Mamba's BDD syntax,
# but this configuration helps with basic test discovery

testpaths = behaviors
python_files = *_test.py test_*.py
python_classes = 
python_functions = 
addopts = -v --tb=short

